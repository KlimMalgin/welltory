/*import { 
    GET_CSRF
    } from '../constants/auth';*/

const initialState = {};

export default function editor(state = initialState, action) {
  switch (action.type) {

    default:
        return state;
  }
}
